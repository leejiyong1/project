<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="b">
	 <!-- <select id="board_list" resultType="board">
		select * from board
	</select> -->
	<select id="board_list" resultType="board" parameterType="java.util.HashMap">
		select * from (select rank() over(order by idx desc) no, b.* from board b) where no between #{start} and #{end}
	</select>
	
	<select id="count_page" resultType="int">
		select count(*) from board
	</select>
	
	<insert id="board_write" parameterType="board">
	 insert into board values(seq_board_idx.nextval,#{title},#{content},#{id},#{ip},sysdate,#{filename})
	</insert>
</mapper>












